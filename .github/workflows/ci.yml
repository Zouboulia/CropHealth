name: CI Workflow  

on:
  push:
    branches:
      - main  # push to the main branch
  pull_request:
    branches:
      - main  # pull requests to the main branch

jobs:
  # Job for building and testing on Windows
  build_windows:
    runs-on: windows-latest  

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up .NET SDK
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '8.0'

    - name: Restore dependencies
      run: dotnet restore

    - name: Build project
      run: dotnet build --configuration Release

    - name: Run tests
      run: dotnet test --configuration Release
      
  # Job for building and testing on Windows
  build_ubuntu:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up .NET SDK
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '8.0'

    # Set working directory to 'SmartCrop' 
    - name: Set working directory to SmartCrop
      run: cd SmartCrop
    
    # Restore dependencies on Ubuntu
    - name: Restore dependencies
      run: dotnet restore SmartCrop.sln 

    # Build project on Ubuntu
    - name: Build project
      run: dotnet build SmartCrop.sln --configuration Release

    # Run tests on Ubuntu
    - name: Run tests
      run: dotnet test SmartCrop.sln --configuration Release

 
